# Курсовая работа №1 «Резервное копирование»

Приветствую, эксперт представляю вам свой вариант курсовой работы по теме Резервное копирование

задание оказалось не очень сложным,но все же были и проблемы.Потратил час на понимание ошибки,казалось что все было правильно,но только потом долшо что неправильно сформировал http запрос к загрузке файла н яндекс диск. Думал сделать из json_data=[] такой вариант json_data=['info':[]] но при обращении в def _json_file_append_info(self,new_info) происходила ошибка: нельзя вызывать append к словарю хотя я обращался вот так: self.json_data..get('info').append(new_info) по идее обращенение идет к значению т.е списку и аппенд должен работать но у меня не получилось и я решил сделать более простым способом. Даже с нейронкойразобраться в этом не сумел. Расчитываю на вашу обратную связь по этому поводу. Спасибо за внимание

*__Для работы программы нужен ваш ключ API. Вам нужно будет создать файл Python, например API_TOKEN.py и и создать переменную API_KEY или любое ваше название. В main нужно импортировать ваш созданный файл таким образом: from API_TOKEN import API_KEY. Дальше передать ваш токен в параметры функции при создании обьекта класса.__*
